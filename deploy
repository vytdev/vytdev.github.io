echo "Deploying to GitHub Pages..."

read -p "Did you want to commit last changes (if any)? [y/n]: " a
if [[ $a == "y" ]];
then
	git stage * && git commit -m "Site deployment"
fi

git push && git subtree push --prefix site origin site


